<%= form_for(expertise_widget_attachment) do |f| %>
  <% if expertise_widget_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expertise_widget_attachment.errors.count, "error") %> prohibited this expertise_widget_attachment from being saved:</h2>

      <ul>
      <% expertise_widget_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :expertise_widget_id %>
    <%= f.number_field :expertise_widget_id %>
  </div>

  <div class="field">
    <%= f.label :photo %>
    <%= f.text_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
