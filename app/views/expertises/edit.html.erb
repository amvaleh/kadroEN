<head>
    <% if Rails.env.production? %>
      <%= render 'shared/externals/gtag_header' %>
      <%= render 'shared/externals/ganalytics' %>
  <% end %>
    <title>
        Edit Shoot Type
    </title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <META NAME="ROBOTS" CONTENT="INDEX, FOLLOW">
  
    
    <link rel="icon" type="image/png" href="/img/shootempire.png">
    <meta name="theme-color" content="RGB(41, 135, 249)" />
  
    <%= csrf_meta_tags %>
   
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'wordpress', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'wordpress', 'data-turbolinks-track': 'reload' %>
  
  
  
    <link href="https://unpkg.com/filepond@^4/dist/filepond.css" rel="stylesheet" />
    <link
    href="https://unpkg.com/filepond-plugin-image-preview/dist/filepond-plugin-image-preview.css"
    rel="stylesheet"/>
  
  </head> 
  
  <script src="https://unpkg.com/filepond-plugin-image-preview/dist/filepond-plugin-image-preview.js"></script>
  <script src="https://unpkg.com/filepond@^4/dist/filepond.js"></script>
  
  <body>
    <section class="container">
      <div class="row">
          <div class="col-12 col-sm-10 mx-auto">
            <h3>
              <%=@shoot_type.title%>
            </h3>
          </div>
      </div>
    </section>  
  
    <section class="container">
      <div class="row" style="margin-bottom: 30px;">
        <div class="col-12 col-sm-8 mx-auto">
          <%= form_tag receive_photo_expertise_path , method: "post", remote:true do %>
          <input type="text" hidden name="expertise_id" value="<%=@expertise.id%>" id="">
          <input type="file" name="file" multiple data-allow-reorder="true" />
          <button type="submit"> submit </button>
          <% end %>
        </div>
        <div class="col-12 col-sm-8 mx-auto">
          <% @expertise.photos.each do |photo| %>
            <img src="<%=photo.file_url(:medium)%>" alt="" class="img-fluid">
          <% end %>

        </div>
      </div>
    </section>
    <%= render "types/sections/mobile_footer"%>
  </body>
  
  
  <script>
      // Get a reference to the file input element

      const inputElement = document.querySelector('input[type="file"]');
  
      FilePond.registerPlugin(FilePondPluginImagePreview);
    
      // Create a FilePond instance

    const pond = FilePond.create(inputElement, {
        storeAsFile: true,
        credits: false,
        maxFileSize: 1024 * 1024 * 20,
        labelMaxFileSizeExceeded: 'Too large',
        labelMaxFileSize: 'largest size is: {filesize} '
      });

      expertiseId = '<%=@expertise.id%>'

      // const pond = FilePond.create(inputElement);


  </script>