<span>
  عکاس
  <span class="owner">
  <% if activity.owner.present? %>     <%= link_to activity.owner.display_name, admin_photographer_path(activity.owner.slug) %>     <% else %>       با آیدی       <%= activity.owner_id %>     که دیگر موجود نیست     <% end %>
  </span>
  <% expertise = Expertise.find_by(id: activity.recipient_id) %>
  <span class="recipient">
  <%= link_to   "یک رشته را",expertise.present? ? admin_expertise_path(activity.recipient_id) : "#"%>
  </span>
  به تخصص های خود اضافه کرد
  <% unless expertise.present? %>
  که بعدا آن را حذف نمود
  <% end %>
</span>