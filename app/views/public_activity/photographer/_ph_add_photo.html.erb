<span>
  عکاس
  <span class="owner">
    <% if activity.owner.present? %>     <%= link_to activity.owner.display_name, admin_photographer_path(activity.owner.slug) %>     <% else %>       با آیدی       <%= activity.owner_id %>     که دیگر موجود نیست     <% end %>
  </span>
  <% photo = Photo.find_by(id: activity.recipient_id) %>
  <span class="recipient">
  <%= link_to "یک فریم نمونه کار", photo.present? ? admin_photo_path(activity.recipient_id) : "#" %>
  </span>
  به تخصص خود اضافه کرد
  <% unless photo.present? %>
  و بعدا آن را حذف کرد
    <% end %>
</span>