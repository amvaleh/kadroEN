<span>
  <span class="owner">
    <% unless activity.owner.present? %>
    یک فرد نا مشخص
    <% else %>
      عکاس
      <%= link_to activity.owner.display_name, admin_photographer_path(activity.owner.slug) %>
    <% end %>
  </span>
  اطلاعات
  <span class="recipient">
  <% if activity.recipient.present? %>
  <%= link_to " این پروژه" ,admin_project_path(activity.recipient.slug) %>
      <% else %>
    پروژه ای که دیگر نیست
    <% end %>
  </span>
  را مشاهده کرد
</span>
