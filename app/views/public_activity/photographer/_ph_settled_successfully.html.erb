<span>
  عکاس
  <span class="owner">
    <% if activity.owner.present? %>     <%= link_to activity.owner.display_name, admin_photographer_path(activity.owner.slug) %>     <% else %>       با آیدی       <%= activity.owner_id %>     که دیگر موجود نیست     <% end %>
  </span>
  حساب خود را تسویه کرد. جزئیات:
  <span class="recipient">
  <%= activity.parameters %>
  </span>
</span>