<span>
  عکاس
  <span class="owner">
  <% if activity.owner.present? %>     <%= link_to activity.owner.display_name, admin_photographer_path(activity.owner.slug) %>     <% else %>       با آیدی       <%= activity.owner_id %>     که دیگر موجود نیست     <% end %>
  </span>
  عکس
  <span class="recipient">
    <% if activity.recipient.present? %>
    <a href="<%= admin_photographer_attachment_path(activity.recipient_id) %>">
  <img style="width: 100px" src="<%= activity.recipient.avatar_url(:medium) %>">
    </a>
      <% else %>
    ی
    <% end %>
  </span>
را برای پروفایل خود آپلود کرد
  <% unless activity.recipient.present? %>
  که بعدا آن را حذف نمود
  <% end %>
</span>