<span>
  عکاس
  <span class="owner">
  <% if activity.owner.present? %>
    <%= link_to activity.owner.display_name, admin_user_path(activity.owner.slug) %>
  <% end %>
  </span>
   آپلود عکس های
  <span class="recipient">
  <%= link_to "پروژه اش " ,admin_project_path(activity.trackable.slug) %>
  </span>
  را تمام کرد.

  <strong class="recipient">
  <%= link_to "گالری", admin_gallery_path(activity.recipient.slug) %>
  </strong>
</span>