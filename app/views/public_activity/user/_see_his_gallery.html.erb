<span>
  کاربر
  <span class="owner">
  <%= link_to activity.owner.display_name.present? ? activity.owner.display_name : 'بی اسم', admin_user_path(activity.owner.slug) %>
  </span>
  <span class="recipient">
  <% if activity.recipient.present? %>
  <%= link_to " از گالری اش" ,admin_gallery_path(activity.recipient.slug) %>
      <% else %>
از گالری اش که دیگر نیست
    <% end %>
  </span>
  دیدن کرد.
</span>