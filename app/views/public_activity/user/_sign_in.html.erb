<span>
  کاربر
<span class="owner">
  <% if activity.owner.display_name.present? %>
  <%= link_to activity.owner.display_name, admin_user_path(activity.owner.slug) %>
  <% else %>
  <%= link_to activity.owner.mobile_number.to_s, admin_user_path(activity.owner.slug) %>
  <% end %>
  </span>
  لاگین کرد
</span>
