<span>
  کاربر
  <span class="owner">
  <%= link_to activity.owner.display_name, admin_user_path(activity.owner.slug) %>
  </span>
  درخواست تمدید
  <span class="recipient">
  <% if activity.recipient.present? %>
  <%= link_to " پروژه اش" ,admin_project_path(activity.recipient.slug) %>
      <% else %>
    پروژه ای که دیگر نیست
    <% end %>
  </span>
  را کرد
</span>