<span>
  کاربر
  <span class="owner">
  <%= link_to activity.owner.display_name, admin_user_path(activity.owner.slug) %>
  </span>
  پکیج
  <span class="recipient">
  <%= link_to activity.recipient.display_name, admin_package_path(activity.recipient_id) %>
  </span>
  را برای پروژه اش انتخاب کرد
</span>