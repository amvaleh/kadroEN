<span>
  کاربر
  <span class="owner">
  <%= link_to activity.owner.display_name, admin_user_path(activity.owner.slug) %>
  </span>

  تمدید
  <span class="recipient">
  <%= link_to " پروژه خود" ,admin_project_path(Project.find_by(id: activity.recipient_id).slug) %>
  </span>
  را با موفقیت پرداخت کرد.
</span>