<% if @shoot_type.relevants.count >= 3 %>
  <div class="container-fluid mt-5">
    <h3 class="text-right mx-5"> مناسب برای: </h3>
    <div class="row text-center justify-content-right no-gutters">
      <% @shoot_type.relevants.each do |sh| %>
        <div class="col-6 col-md-4 m-auto">
          <h3 class="text-center"><strong><%=sh.title%></strong></h3>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
